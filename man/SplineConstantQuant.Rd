% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constant-regression.R
\name{SplineConstantQuant}
\alias{SplineConstantQuant}
\title{'
Performs quantile/mean regression using constant B-splines with monotonicity constraints.
No solver is needed, since for the degree 0, the B-splines in the basis are
independent piece-wise constants.}
\usage{
SplineConstantQuant(
  xtab,
  ytab,
  knot = NULL,
  tau = 0.5,
  monot = 0,
  weight = NULL,
  verbose = FALSE,
  type_reg = "quantile"
)
}
\arguments{
\item{xtab}{Predictor vector (x)}

\item{ytab}{Response vector (y)}

\item{knot}{Knot vector or number of knots}

\item{tau}{Quantile (between 0 and 1)}

\item{monot}{Monotonicity constraint vector: Monotonicity is expressed at knots
1 || k>0 = increasing, -1 ||k<0 = decreasing, 0 = unconstrained.
kn-1 constraints are taken into account, kn=length(knot)-1
If monot is a single number, it is repaeated as uniform coonstraint.
if length(monot)<kn-1 then it is completed with zeros.}

\item{weight}{Observation weights (default = 1 for all)}

\item{verbose}{logical; if TRUE, print progress messages}

\item{type_reg}{'quantile' or 'mean_square' type of regression
i.e form of the objective.}
}
\value{
A list containing coefficients, degree, and knots
}
\description{
'
Performs quantile/mean regression using constant B-splines with monotonicity constraints.
No solver is needed, since for the degree 0, the B-splines in the basis are
independent piece-wise constants.
}
