% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deriv-coeffs.R
\name{bspline_to_deriv_coeffs_cubic}
\alias{bspline_to_deriv_coeffs_cubic}
\title{normalized first and second derivative coefficients on each interval.
for a cubic B-spline basis}
\usage{
bspline_to_deriv_coeffs_cubic(
  tn = NULL,
  degree = 3,
  x_values = 0,
  Bsbasis = NULL,
  verbose = FALSE
)
}
\arguments{
\item{tn}{Knot vector (effective partition, not extended. OPtionnal if Bsbasis
is given)}

\item{degree}{Spline degree (default = 3)}

\item{x_values}{Evaluation points for design matrix (0 = no evaluation)}

\item{Bsbasis}{Bspline basis structure if already computed. Optional if knots are given.}

\item{verbose}{boolean FALSE (default) or TRUE.}
}
\value{
A list containing (kn: Nb intervals, N=kn+3: Nb basis functions):
  \item{d0}{Design matrix (if x_values provided)}
  \item{d1}{First derivative coefficients [a3, a2, a1] for each interval: (kn x N x 3) array}
  \item{d2}{Second derivative values at knot: ((kn+1) x N) matrix}
  \item{d3}{Third derivative values at knots: (kn x N) matrix}
}
\description{
normalized first and second derivative coefficients on each interval.
for a cubic B-spline basis
}
